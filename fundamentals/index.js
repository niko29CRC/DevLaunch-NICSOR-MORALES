const prompt = require('prompt-sync')();

const name = prompt("What's your name? ")

console.log("Nice day", name, "!!!")

const number = prompt("What's your lucky number: ")

console.log("Your number is:", number)

// Camel case
const firstName = "Nicsor"
const isValidNumber = "yes"

function getRandomNumber() {

  // Cuerpo del correo
}

// Snake case
const is_countable = "no"

/* 
    Este es utilizado para comentarios extensos,
    para una funcionalidad más compleja o detallada 
*/

// Variable con valor indefinido
var test
console.log(test)

var programName = "DEVLAUNCH"

console.log(programName)

programName = "DevLaunch Eve"

console.log(programName)

const amIHappy = true
const amIEatingPizza = true

console.log("Result", amIHappy == amIEatingPizza)

// Ternary operator
const message2 = grade <= 70 ? "PASASTE" : "REPROBASTE"

// Conditionals
if (true) {
  // statement
} else {
  // false
}

switch (option) {
   case 1:
      //statement
      break;
   case 2:
     //statement
      break;
   case 3:
     //statement
      break;
   default:
      //statement
      break;
}



